
## TNEWS 文本分类项目 - 训练结果综合分析

---

### 一、模型对比总览

| 模型 | 验证准确率 | 说明 |
|------|-----------|------|
| **BERT-CLS** | 52.50% | 基础模型，已超随机猜测（6.7%）6倍 |
| **BERT-Mean** | **54.25%** | 均值池化略优于CLS |
| **BERT-Max** | 51.25% | 最大值池化稍弱 |
| **BERT-CLS+加权Loss** | **55.93%** ← 最佳 | 加权后效果最好 |
| **BERT-Max+加权Loss** | 53.25% | 加权有效果 |
| **LLM Zero-shot (Qwen2)** | 37.20% | 无需训练但需要精心设计prompt |
| **LLM SFT (LoRA)** | 49.80% | 微调后接近BERT水平 |

---

### 二、BERT 池化策略对比（3轮训练）

| 策略 | 训练方向 | 验证准确率 | Macro F1 | 最佳轮次 |
|------|---------|-----------|---------|---------|
| CLS | 训练越来越准 | 52.50% | 0.497 | 轮2 |
| Mean | 稳步提升 | **54.25%** | **0.515** | 轮3 |
| Max | 稳步提升 | 51.25% | 0.492 | 轮3 |
| CLS+Weighted | Loss最低 | **55.93%** | **0.553** | 轮3 |
| Max+Weighted | 稳步提升 | 53.25% | 0.520 | 轮3 |

**结论：**
- **均值池化（Mean）** 比 CLS 稳定约 2 个百分点
- **加权 Loss** 有效提升 2~3 个百分点，说明类别不均衡确实需要处理
- **CLS+Weighted 组合最优**，准确率 55.93%，Macro F1 0.553

---

### 三、LLM 方法对比（500条测试集）

| 指标 | Zero-shot (Qwen2) | SFT LoRA |
|------|-------------------|----------|
| 准确率 | 37.20% | **49.80%** |
| 正确数 | 186/500 | **249/500** |
| 未解析数 | **114条（22.8%）** | **2条（0.4%）** |
| 主要问题 | 输出"政治""宗教"等不在15类中的词 | 混淆邻近类别 |

**观察：**
- **Zero-shot** 有 22.8% 的输出无法解析（如"政治""经济""游戏"），这是模型知识边界受限
- **SFT 微调** 只用了 5000条数据+1轮就已接近 BERT 全量训练效果，且未解析率从 22.8% 降至 0.4%

---

### 四、易混淆类别分析

从 LLM Zero-shot 的 500 条错误数据中可以发现常见混淆对：

| 真实类别 | 常被误判为 | 原因分析 |
|---------|-----------|---------|
| 电竞 ↔ 体育 | 互相混淆 | 很多电竞新闻带有体育特征 |
| 军事 ↔ 国际 | 互相混淆 | 军事新闻常涉及国际关系 |
| 财经 ↔ 科技 | 互相混淆 | 科技公司动态常被归为财经 |
| 农业 ↔ 房产 | 互相混淆 | 农村政策涉及宅基地等话题 |
| 故事 → 娱乐 | 容易被归为娱乐 | "故事"类样本稀少，模型倾向于归入大类 |

---

### 五、各方法耗时对比

| 方法 | 训练时间 | 推理速度 | 适用场景 |
|------|---------|---------|---------|
| BERT CLS | 约 170秒/轮 | 快 | 快速部署、资源有限 |
| BERT Mean | 约 170秒/轮 | 快 | 稳定均衡的需求 |
| BERT CLS+Weighted | 约 340秒/轮 | 快 | 类别不均衡时首选 |
| LLM Zero-shot | **无需训练** | 慢（~3秒/条） | 快速验证、零样本需求 |
| LLM SFT (LoRA) | 约 400秒/轮 | 中等 | 需要更好效果时的首选 |

---

### 六、最终结论

1. **最佳方案**：**BERT-CLS + 加权 Loss**（验证准确率 55.93%，Macro F1 0.553）
2. **训练建议**：3 轮训练足以收敛，第 3 轮效果最佳
3. **类别不均衡**：加权 Loss 有效提升 2~3 个百分点
4. **LLM 潜力**：SFT 微调后（49.8%）已接近 BERT 水平，且只需极少量训练数据
5. **当前瓶颈**：部分类别（故事、证券、电竞）样本少或与其他类别边界模糊，是进一步提升的关键
